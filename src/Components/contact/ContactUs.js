import React from 'react';
import Form from './Form';

const Contact = ({ name, label, value, onChange }) => {
  return(
    <div className="contact">
      <Form />
    </div>
  );
};

export default Contact;